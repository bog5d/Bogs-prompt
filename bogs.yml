matches:
  - trigger: ":ksrj"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "powershell.exe -NoProfile -Command \"(Invoke-RestMethod -Uri 'https://raw.githubusercontent.com/bog5d/Bogs-prompt/main/ksrj.md')\""

  - trigger: ":kych"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "powershell.exe -NoProfile -Command \"(Invoke-RestMethod -Uri 'https://raw.githubusercontent.com/bog5d/Bogs-prompt/main/kych.md')\""

  - trigger: ":smqpft"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "powershell.exe -NoProfile -Command \"(Invoke-RestMethod -Uri 'https://raw.githubusercontent.com/bog5d/Bogs-prompt/main/smqpft.md')\""

  - trigger: ":tysk"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "powershell.exe -NoProfile -Command \"(Invoke-RestMethod -Uri 'https://raw.githubusercontent.com/bog5d/Bogs-prompt/main/tysk.md')\""

  - trigger: ":syrj"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "powershell.exe -NoProfile -Command \"(Invoke-RestMethod -Uri 'https://raw.githubusercontent.com/bog5d/Bogs-prompt/main/syrj.md')\""
